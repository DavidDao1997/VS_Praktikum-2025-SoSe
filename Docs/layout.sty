% Codierung
\usepackage{selinput}
\SelectInputMappings{
  adieresis={ä},
  germandbls={ß},
}
\usepackage[english,ngerman]{babel}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

% Kopf-/Fusszeilen
\usepackage{scrlayer-scrpage}
\usepackage{lastpage}
\setkomafont{pageheadfoot}{\small\sffamily}
\pagestyle{scrheadings}
\lohead*{}
\cohead*{}
\rohead*{}
\lofoot*{}
\cofoot*{}
\rofoot*{Seite \thepage \hspace{1pt} von \pageref{LastPage}}

% Bilder
\usepackage{graphicx}

% Tabellen
\renewcommand{\arraystretch}{1.5}
\usepackage[table]{xcolor}
\usepackage{longtable}
\usepackage{tabularx}

% Abkürzungen
\usepackage{acronym}

% Hyperlinks
\usepackage[hidelinks]{hyperref}

% Glossar (Nach hyperref laden!)
\usepackage[xindy,toc]{glossaries}

% Querformat
\usepackage{pdflscape}

% Symbole
\usepackage{wasysym}

% Dummy-Text
\usepackage{blindtext}

% Quellcode
\definecolor{keywordcolor}{rgb}{0.5,0,0.35}				% keyword color
\definecolor{stringcolor}{rgb}{0.6,0,0}					% string color
\definecolor{commentcolor}{rgb}{0.25,0.5,0.35}			% comment color
\definecolor{javadoccolor}{rgb}{0.25,0.35,0.75} 		% javadoc color

\definecolor{lightyellow}{rgb}{1.0, 1.0, 0.5}  % Light Yellow
\definecolor{lightblue}{rgb}{0.5, 0.5, 1.0}    % Light Blue
\definecolor{lightgreen}{rgb}{0.5, 1.0, 0.5}   % Light Green
\definecolor{teal}{rgb}{0.0, 0.5, 0.5}         % Teal
\definecolor{darkbg}{rgb}{0.12, 0.12, 0.12}    % Dark gray background

\usepackage{listings}
\usepackage{xcolor}

% Define Java language (minimal)
\lstdefinelanguage{Java}{
  morekeywords={class, interface, enum, throws},
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[s][\color{commentcolor}]{/**}{*/},  % Javadoc comment coloring
  morestring=[b]"
}

% Listings Style für Quellcode
\lstset{
  float,
  basicstyle=\ttfamily\small,                               % Use monospaced font for code
  keywordstyle=\color{blue}\bfseries,                        % Blue for keywords
  commentstyle=\color{green},                                % Green for comments
  stringstyle=\color{red},                                   % Red for strings
  showstringspaces=false,                                    % Don't show spaces in strings
  breaklines=true,                                           % Automatically break long lines
  frame=single,                                              % Frame around the code block
  backgroundcolor=\color{gray!10},                           % Light gray background for the code
  captionpos=b,                                              % Caption position at the bottom
  tabsize=2,                                                 % Tab size set to 2 for consistent formatting
  numberstyle=\tiny\color{gray},                             % Line numbers in small gray font
  stepnumber=1,                                              % Number every line
  numbers=left,                                              % Place line numbers on the left
  numbersep=5pt,                                             % Distance of line numbers from the code
}

\usepackage{caption}
% \captionsetup[lstlisting]{labelformat=empty}
\captionsetup[lstlisting]{name=Snippet}


% Listings Style für Java
\lstnewenvironment{Java}[1][]{
  \lstset{
    float,
    #1,                               % Dynamically set the caption
    language=Java,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{lightblue},   % Light yellow for method names
    % identifiers
    identifierstyle=\color{teal!50},  % Light blue for arguments and parameters
    morekeywords={[2]Position, RobotArm, Movement, UI, IO, ActuatorController, CouldNotReachRobotException},
    keywordstyle={[2]\color{teal!50}},
    % properties
	  morekeywords={[3]
      Position, rotation, joint1, joint2, claw, 
      RobotArm, name, id,
      Movement, ROTATE_CLOCKWISE, ROTATE_COUNTERCLOCKWISE, JOINT1_OPEN, JOINT1_CLOSE, JOINT2_OPEN, JOINT2_CLOSE, CLAW_OPEN, CLAW_CLOSE
    },
    keywordstyle={[3]\color{white}},
    % parameters
	  morekeywords={[4]robotArms, robotArm , position, message, callback},
    keywordstyle={[4]\color{lightblue!50}},
	  % methods
    morekeywords={[5]displaySelectOptions, displaySelected, displayArmStatus, displayError, onMoveCommand, onSelect, move, getPosition}, % Add methods to keyword list
    keywordstyle={[5]\color{lightyellow}},
    commentstyle=\color{commentcolor},                % comment color
    stringstyle=\color{stringcolor},                  % string color
    showstringspaces=false,                           % show spaces in a string
    breaklines=true,                                  % print linebreaks
    frame=single,                                     % frame
    backgroundcolor=\color{darkbg},                   % background color
	  literate={\{}{{\color{white}\{}}1 {\}}{{\color{white}\}}}1,
  }
}{}

% PDF einbinden
\usepackage{pdfpages}
\includepdfset{pages=-,noautoscale}
