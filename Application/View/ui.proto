syntax = "proto3";

package robotview;

// Nachricht zum Updaten der View
message ViewUpdate {
  repeated string available_robots = 1;   // Liste der Roboternamen, inkl. "None"
  int32 selected_robot_idx = 2;          // Index des aktuell ausgewählten Roboters (0 = None)
  bool error = 3;                        // Fehlerstatus
  bool confirmation = 4;                 // Erfolgsstatus
}